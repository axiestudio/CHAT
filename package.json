{
  "name": "axiestudio-ai-flow-generator",
  "version": "1.0.0",
  "description": "AxieStudio AI Flow Generator - Full Stack Application",
  "scripts": {
    "build": "echo 'Building from root - this should not be called'",
    "start": "cd frontend && npm start",
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && pip install -r requirements.txt"
  },
  "keywords": [
    "axiestudio",
    "ai",
    "flow-generator",
    "react",
    "fastapi"
  ],
  "author": "AxieStudio Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
